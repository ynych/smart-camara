{
  "private": true,
  "name": "smart-camara",
  "description": "根目录无前端依赖；请使用 ./start.sh 或在 frontend/ 下执行 npm install",
  "scripts": {
    "install:frontend": "npm install --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
